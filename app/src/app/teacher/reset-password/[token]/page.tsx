"use client";
import React, { JSX } from 'react';

// Temporarily simplify the Page component to isolate the type error.
export default function Page(): JSX.Element {
    return <div>Reset Password Page</div>;
}