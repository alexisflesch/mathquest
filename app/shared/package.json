{
    "name": "shared",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "type-check": "tsc --project tsconfig.json --noEmit --pretty",
        "lint": "eslint . --ext .ts"
    },
    "devDependencies": {
        "@types/node": "^20",
        "typescript": "^5.8.3",
        "eslint": "^9"
    }
}